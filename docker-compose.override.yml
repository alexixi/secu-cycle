services:
  db:
    ports:
      - "5433:5432"
  api:
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - ./backend/graphs:/app/graphs
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
  web:
    ports:
      - "8080:80"
